// PHASE 3 — Minimum Intervention Engine
//
// Not implemented in Phase 1. Will compare counterfactual futures
// (do nothing / alternative / minimum intervention) and recommend the
// smallest action that meaningfully changes the simulated outcome.

/**
 * Will return the smallest intervention that prevents or reduces the
 * simulated cascading impact for a given scenario.
 */
export function recommendMinimumIntervention() {
  throw new Error('interventionEngine.recommendMinimumIntervention is implemented in Phase 3.')
}
